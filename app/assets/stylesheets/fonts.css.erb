@font-face {
  font-family: 'Homestead';
  src:url('<%= font_path(Homestead-Display.ttf) %>') format('truetype');
  src:url('<%= font_path(Homestead-Inline.ttf) %>') format('truetype');
  src:url('<%= font_path(Homestead-One.ttf) %>') format('truetype');
  src:url('<%= font_path(Homestead-Regular.ttf) %>') format('truetype');
  src:url('<%= font_path(Homestead-Three.ttf) %>') format('truetype');
  src:url('<%= font_path(Homestead-Two.ttf) %>') format('truetype');
  font-weight: normal;
  font-style: normal;
}
